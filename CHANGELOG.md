# 更新记录

BalanceHub 的重要变更会记录在这里。

## 0.5.7

### 新增

- 每个 Agent CLI 的默认配置可以独立绑定当前中转站的任意一把 API Key；切换默认配置时先选择 Key，配置预览会展示所选 Key，卡片上的 Agent 轨道图标也会提示各自绑定的 Key。
- 中转站卡片头部新增 API Key 管理与复制入口，可直接进入密钥库或复制当前调用 Key。

### 变更

- API Key 管理从独立弹窗移入“编辑中转站 → 认证凭据”，同步、添加、创建、备注、复制、删除和当前调用 Key 切换都在同一页完成并即时保存，不再叠加第二个管理弹窗。
- 0.5.6 的“主 Key”统一改称“当前调用 Key”，只负责卡片刷新、模型请求和未单独指定 Key 的临时 CLI；Agent CLI 的绑定不再受它限制。
- 纯 API Key 认证的卡片只维护本地 Key，不再展示没有凭据来源的站点同步与创建操作。
- 已被 Agent 默认配置绑定的 Key 不能直接删除，需要先为对应 Agent 切换绑定。
- 已设为某个 Agent 默认中转站的卡片仍可再次进入该 Agent 的配置，用于更换绑定的 Key。

### 修复

- 修复站点同步或配置归一化时，过期的 Token ID 可能顶掉当前实际使用的完整 API Key 的问题；现在以完整 Key 为准回填站点元数据。
- 修复删除当前调用 Key 后卡片直接失去可用凭据的问题，现在会自动接续下一把可用 Key。
- 账号凭据已失效时，仍可把站点 Key 仅从当前卡片移除，不会因此撤销站点令牌。
- 修复取消 CLI 配置选择或切换卡片后，较晚返回的配置预览与密钥操作结果仍可能写入当前界面的问题。

## 0.5.6

### 新增

- 新增中转站与通用 API Key 卡片备注，可在卡片、弹窗、托盘、通知、CLI 配置和运行实例等位置统一使用备注后的展示名称。
- 同一张卡片内的每个 API Key 支持独立本地备注，可新增、修改或清空；搜索、主 Key 展示、临时 CLI 选择与启动预览均优先使用对应备注。

### 变更

- 将 API Key 的本地备注与站点返回名称分开维护；本地备注作为主名称，站点名称在两者不同时作为辅助信息展示。
- 后台任务入口保持固定状态图标，运行中改用缓慢的描边渐变流动提示，不再切换为旋转加载图标；减少动态效果时自动静态降级。
- API Key 卡片补充主 Key 名称与备注层级，并统一模型、用量、日志、签到、测活、公告和临时 CLI 等界面的中转站名称规则。

### 修复

- 修复站点同步返回完整 Key、Token ID 变化或重复记录合并时，本地 Key 备注和稳定身份可能丢失的问题。
- 修复密钥库快速关闭或切换卡片后，较晚返回的列表、创建或删除结果可能写入当前界面的问题。
- 避免保存未变化的 Key 备注时重复写盘和增加配置 revision，并移除手动加入 Key 时自动生成的伪备注。

## 0.5.5

### 新增

- 新增 Agent 历史会话全文检索，按 Agent 使用独立 SQLite 索引，并支持从设置中控制索引开关、容量与存储位置。
- 新增历史会话详情视图，以对话流形式展示用户、Agent 与工具交互；检索结果保持为会话列表，详情按需加载。
- 新增中转站多 API Key 管理，可查看本地与站点 Key、设置本地名称、切换主 Key，以及单独加入或移除本地 Key。

### 变更

- 会话索引改为后台流式构建并受共享容量限制，忽略工具输出、隐藏推理及无有效内容的空会话，减少磁盘占用和前台搜索阻塞。
- 统一 Codex CLI、Claude Code、Gemini CLI 与 Grok Build 的会话摘要、标题、模型、时间、搜索和详情契约，并加强目录切换与异步结果防过期处理。
- 优化同地址中转站的重复校验：相同凭据继续阻止重复保存，不同 API Key 可选择并入已有卡片或创建独立卡片；已有卡片内可明确查看和维护全部 Key。
- API Key 使用稳定的本地身份保存名称与选择状态，站点刷新、Token ID 变化或仅返回脱敏值时不再丢失本地管理信息。

### 修复

- 修复 AnyRouter 及旧版 NewAPI 分支使用零起始分页时，API Key 列表被误读为空、创建成功后又被误报失败的问题；列表请求改为兼容新旧版本的第一页契约。
- 修复 API Key 已提交创建但后续列表刷新失败时提示不明确、用户重复操作可能产生重复 Key 的问题。
- 修复历史会话搜索直接铺开消息片段、工具调用进入检索范围，以及切换目录或会话后旧结果回写当前界面的问题。

## 0.5.4

### 修复

- 修复启动临时 CLI 时启动命令、终端自动化或状态轮询阻塞前端，导致工作空间弹窗、主面板和其他操作全部置灰的问题。
- 修复临时 CLI 启动预览因 Vue 深层响应式代理改变对象身份，导致预览结果无法正确进入确认阶段的问题。
- 修复默认 CLI 配置保存和临时 CLI 运行状态读取缺少超时、确认后持续锁定界面的问题；相关操作改为可关闭弹窗并在后台完成。

### 变更

- 新增异步 UI 并发约束和自动回归检查，禁止普通异步操作锁死模态窗口，禁止使用响应式对象身份比较作为过期结果判断。
- 临时 CLI、CLI 配置切换、运行实例刷新和终端激活统一使用有界等待，并在失败或超时后释放忙碌状态。

## 0.5.3

### 新增

- 新增顶部活动临时 CLI 汇总，仅在存在运行实例时按 Agent 展示图标与数量，并可直接打开该 Agent 的全部活动实例。
- 新增 NewAPI 与 Sub2API 站点公告中心，按协议与站点域名聚合公告，支持未读数量、单条已读、全部已读和十五分钟后台刷新。
- 新增统一后台任务中心，集中展示全局刷新、签到、公告、版本更新、Agent CLI 检测、能力探测以及自动刷新、自动签到和自动测活的当前动作、进度与结果。

### 变更

- 临时 CLI 实例补充会话标题、中转站和账号来源，并收紧本地运行记录目录权限；历史会话标题在启动时固定快照，避免后续索引变化导致展示漂移。
- 公告按站点而非本地账号去重，同站点存在多个账号时自动选择可用凭据读取；公告读取失败不提前重试，也不在顶部制造失败红点。
- 后台任务结果改为短期待查看记录，同类结果只保留最新一条，最多保留十二条并在十五分钟后过期；查看后可自动清除，也可手动清空。
- 顶部版本号改为更新按钮角标，公告未读使用独立红色角标；后台任务失败详情仅在任务面板内部展示。

### 修复

- 修复后台自动任务、公告预取和 CLI 探测缺少统一状态反馈，执行期间容易被误认为界面卡顿的问题。
- 修复同一站点多个账号重复读取和重复展示公告，以及公告已读后角标未及时消失的问题。

## 0.5.2

### 变更

- 将 NewAPI、Sub2API 和通用 API 的协议身份、认证 Schema、能力描述与运行时分发收敛到 Rust 协议注册表，前端统一消费 IPC 描述，不再维护重复的协议规则。
- 拆分中转站服务、配置存储、临时 CLI、平台终端和前端工作空间编排，核心入口仅保留调度职责，降低新增协议、终端和业务能力时的修改范围。
- 将前端设置、工作空间和 CLI 运行状态迁移到独立 Pinia store，并拆分目录浏览、会话历史、API Key 选择与启动流程。
- 统一终端 catalog、平台探测、Shell 环境和启动脚本边界，macOS、Linux 与 Windows 复用同一套临时 CLI 编排契约。
- 强化中转站异步请求的 revision 与事务回写约束，避免过期结果覆盖已编辑、已切换认证或已删除的卡片。

### 修复

- 修复协议探测、认证补全、API Key 同步和刷新结果在并发配置变化下可能沿用旧状态的问题。
- 修复临时 CLI 脚本拆分后 Linux 与 Windows 条件编译导入不准确的问题。

## 0.5.1

### 新增

- 新增统一 Agent CLI 注册表与能力适配层，内置 Codex CLI、Claude Code、Gemini CLI 和 Grok Build，并按各自官方能力提供路径发现、临时启动、模型选择、历史会话、测活和默认配置管理。
- 新增 Gemini CLI 与 Grok Build 的历史会话读取、指定会话恢复、模型与 Token 统计、默认配置预览编辑及切换能力。
- 新增通用 Agent 图标组件和卡片边缘轨道展示；设置页 Agent 卡片改为响应式排列，名称下仅展示版本号，完整版本与可执行路径通过悬浮提示查看。

### 变更

- Codex CLI 与 Claude Code 迁移到统一 Agent CLI 架构，临时 CLI、测活、会话、配置、可用模型和路径扫描改为复用同一份 Rust 能力契约与动态前端结构。
- 新增 Agent 时只需登记 catalog、实现对应能力 Adapter 并补充前端视觉资源；移除原 Codex、Claude Code 专用的重复编排、配置、会话和测活实现。
- 会话列表根据 Agent 的实际数据源解析标题、模型、时间和 Resume ID，过滤无有效内容的空会话，并根据能力动态展示新会话、命名与恢复入口。
- 统一官方产品名称为 Codex CLI、Claude Code、Gemini CLI 和 Grok Build，并同步更新 README、Pages、使用说明和架构文档。

### 修复

- 修复 Claude Code 会话标题误取首条短消息、不同 Agent 路径与环境变量硬编码，以及新增 Agent 时需要修改多处通用编排的问题。
- 修复 Agent 默认配置、临时 CLI 预览和测活流程中部分能力状态、Base URL、模型与配置路径未按所选 Agent 动态处理的问题。
- 将传递依赖 `nanoid` 升级至 3.3.18，修复自定义生成器在零长度输入下可能无限循环的高危依赖问题。

## 0.5.0

### 修复

- 修复 macOS 临时 CLI 启动脚本使用 zsh 保留变量 `status` 导致启动后直接报错的问题。
- 修复 Windows 临时 CLI 批处理脚本使用通用 `STATUS` 变量的问题，统一使用项目命名空间变量。

### 变更

- 新增 `npm run doctor` 应用自检入口，覆盖平台脚本变量、缓存体量、前端构建测试、Rust 格式、Clippy 和 Rust 测试。
- 新增 `npm run doctor:platform` 快速检查平台变量和构建缓存。
- 本地 `npm run tauri dev/build` 默认将 Cargo target 隔离到系统开发缓存目录，避免仓库内构建缓存持续膨胀。
- 增加平台脚本变量命名约束和临时 CLI zsh 回归测试，并让 CI/Release 构建缓存路径与 Rust 缓存配置保持一致。

## 0.4.9

### 修复

- 修复 NewAPI 普通签到在 Cookie 失效后不会自动回退访问令牌的问题，并补齐凭据补全、签到记录、密钥管理、用量和邀请链接的统一认证回退。
- 修复 AnyRouter 签到响应缺少成功标记时被误判为成功的问题，保留服务端返回的认证错误详情。
- 修复 Sub2API 访问令牌过期、刷新令牌轮换、重新登录和凭据持久化链路，避免并发操作重复消费同一个刷新令牌。
- 修复 NewAPI API Key 额度接口使用 `code: true` 时被误判为响应格式错误的问题。
- 修复凭据、地址和协议变更后继续使用旧能力缓存、模型列表和派生身份信息的问题。
- 修复 macOS 代理回退实现被非 macOS 质量检查误报未使用的问题。

### 变更

- 统一认证操作结果与当前 Provider 快照的原子写回，避免旧账号请求覆盖用户刚修改的配置。
- 按 macOS、Windows、Linux 使用对应的浏览器 User-Agent，并补充 macOS 网络服务代理回退读取。

## 0.4.8

### 新增

- 新增全局刷新和一键签到进度面板，按中转站展示执行阶段、成功数据和失败原因，并支持刷新可用模型。
- 新增 CLI Agent 图标环绕轨道，支持未来 Agent 使用通用图标；每个图标拥有独立的随机周期和速度，并在周期结束后重新抽取参数。
- 新增应用版本信息与 GitHub 入口展示，更新检查状态改为独立的加载图标反馈。

### 变更

- 统一中转站刷新、签到、模型同步与令牌更新链路，复用并发控制、状态回写和进度事件。
- 优化顶部操作区、搜索筛选、模型预览和卡片信息布局，移除无效筛选入口与无意义的数量展示。
- 完善 Codex、Claude Code 及临时 CLI 的跨平台环境加载、路径探测和终端启动处理，减少桌面环境与登录 Shell 的差异。
- CLI Agent 图标改为紧凑队列沿卡片边框运行，图标轨道与普通卡片流光解耦，减少动画重叠和高频重绘。

### 修复

- 修复 Sub2API 访问令牌过期后未自动使用刷新令牌更新凭据的问题。
- 修复全局刷新时卡片未持续显示同步状态、模型刷新未覆盖 API Key 卡片等状态链路问题。
- 修复批量操作结果统计、卸载后的异步回写以及部分 CLI 环境和平台命令边界问题。

## 0.4.7

### 新增

- 新增临时 CLI 启动前确认预览，展示实际 CLI、终端、工作目录、模型、环境变量和完整命令，并支持一键复制命令。
- 新增 Codex 与 Claude Code 历史会话读取，展示标题、摘要、Resume ID、模型和时间，可直接继续指定会话并按需覆盖模型。

### 变更

- 重做临时 CLI 工作空间选择器布局，将工作空间、终端和 AI 启动配置分区展示；历史会话模式扩大列表区域并过滤空会话。
- 临时 CLI 启动兼容 Unix 登录 Shell 环境，以及 Windows 当前进程、cmd 和 PowerShell 配置中的环境变量、别名和函数。

### 修复

- 修复临时 CLI 启动预览与实际启动参数不一致、历史会话恢复参数缺失，以及部分提示组件内容未正确渲染的问题。

## 0.4.6

### 新增

- 新增中转站重复校验：账号协议按规范化域名与用户 ID 识别同账号，API Key 协议按地址与 Key 识别重复，并支持在同地址下追加不同 API Key。
- 新增顶部统一搜索，可按中转站名称、地址、账号、用户 ID、CLI 模型和可用模型筛选，同时保留认证方式与状态筛选。
- 新增 CLI 配置完整编辑器，在同一编辑器内展示原配置与新配置的逐行增删差异，并保留可编辑内容。

### 变更

- CLI 配置切换改为提交编辑器中的完整文件内容；保存前校验文件集合、JSON/TOML 格式和配置版本，避免覆盖并发修改。
- 卡片状态统一使用从右上角向左下角的轻量渐变和边框流光交互，顶部操作区重新排版，并恢复各协议的原生图标。
- 移除无效的通用 Cloudflare 过盾实现，仅保留 AnyRouter 使用的确定性阿里云 WAF 处理，简化盾状态和凭证管理。

### 修复

- 修复 Claude 配置切换会重排整个 `settings.json`、Codex 认证文件产生无关差异的问题，改为保留源文件排版并只写入目标字段。
- 修复搜索误匹配 API Key、Token、Cookie 等敏感凭据的问题，并补充 Provider 搜索与 CLI 配置排版测试。
- 修复跨平台网络、单实例、托盘、更新器和临时 CLI 边界处理中的资源回收、代理复用和状态回写问题。

## 0.4.5

### 新增

- 新增统一过盾流程，支持阿里云 WAF 与 Cloudflare 挑战的凭证缓存、挑战状态提示和用户主动验证窗口。
- 临时 CLI 启动过程增加阶段进度反馈，并按 CLI 官方能力支持新会话命名、继续最近会话或进入官方恢复选择器。

### 变更

- NewAPI、Sub2API、通用 OpenAI API 与 Codex 模型同步统一使用 `ProviderTransport`，业务 Cookie 与过盾 Cookie 分离管理。
- 仅对 GET、HEAD、OPTIONS 做有限自动重试；POST、PUT、PATCH、DELETE 命中挑战后不自动重放，避免重复执行有副作用的请求。
- 过盾凭证按中转站、站点 origin、代理路由和盾类型隔离，并增加容量、TTL、并发窗口和失效竞态保护。

### 修复

- 修复 Cloudflare 挑战页面识别、Cookie 域过滤、隐藏 WebView 回收和并发凭证失效问题。
- 修复挑战状态变化后卡片操作菜单不能及时更新的问题，并明确拒绝无法保证 DNS/出口一致性的 `socks5h` WebView 路由。

## 0.4.4

### 新增

- 临时 CLI 启动前可按当前工作目录读取 Codex 与 Claude Code 历史会话，展示标题、Resume ID、模型和时间，并直接继续指定会话。

### 变更

- 优化异常中转站卡片的状态渐变方向，移除左侧状态线。
- 优化活动临时 CLI 弹窗布局，固定实例卡片尺寸并将 Agent 与终端信息并列展示。

## 0.4.3

### 新增

- 新增应用单实例保护，第二次启动只唤起主窗口，避免后台调度任务重复运行。
- 新增更新下载进度、取消与阶段状态，启动 30 秒后静默检查，之后每 6 小时检查一次。
- 新增前端 LRU 与资源边界测试，并将 Linux、Windows、macOS 纳入日常 Rust 质量检查矩阵。

### 变更

- 按桌面入口、命令、网络、存储、CLI 运行时、临时终端和平台实现拆分 Rust 核心模块，降低单文件复杂度。
- 统一业务请求、Webhook、updater 与 CLI 的代理语义，支持 SOCKS、系统代理和系统证书存储。
- HTTP Client、站点元数据和签到记录改为有容量上限的 LRU；模型、API Key、通知渠道、测活与签到历史等持久化集合增加统一资源上限。
- 配置保存改为流式序列化和短写锁事务，自动刷新、签到、测活与通知使用有界并发。
- 大型模型列表限制实际渲染数量，设置页不再构建全量“模型 × 中转站”索引，降低大量站点场景下的内存和布局开销。

### 修复

- 修复 Windows 环境变量名大小写不敏感时代理环境回归测试误判，确保三端发布质量门禁一致。
- 修复文件、HTTP/Webhook 响应、CLI 输出和更新包可无界读入内存的问题。
- 修复测活与后台命令超时后可能遗留子进程、Windows 后台命令弹出控制台以及输出管道长期等待的问题。
- 修复 updater 检查或下载长期无响应、重复安装、取消边界不清晰，以及重启继承静默启动参数的问题。
- 修复 Windows CC Switch 深链启动、Linux 托盘不可用时窗口无法恢复，以及 updater 重启参数处理不一致的问题。
- 修复更新检查、窗口吸附、拖拽、模型预览、设置保存和应用事件监听在组件卸载后仍可能继续回写或遗留定时器的问题。

## 0.4.1

### 修复

- 修复 macOS 专用终端辅助函数在 Linux 和 Windows 发布检查中触发 Clippy `dead_code`，导致 `0.4.0` 未通过质量门禁的问题。
- 更新 Rust 网络传递依赖，修复 `quinn-proto` 已知高危安全问题。

## 0.4.0

### 新增

- 新增 Sub2API 协议接入，支持账号认证、令牌刷新、用户资料、API Key 管理、额度与用量查询。
- 新增通用 OpenAI 兼容 API 接入，通过真实 `/v1/models` 请求识别协议并同步可用模型。
- 新增中转站能力探测，可分别检查模型列表、密钥管理和邀请链接，并展示具体失败原因。
- 新增顶部状态筛选、CLI 类型筛选和临时 CLI 弹窗，支持按中转站、CLI、模型、API Key 与工作空间启动。

### 变更

- 重构中转站协议适配层，统一 NewAPI、Sub2API 与通用 API 的认证、请求和状态写回流程。
- 优化 API Key 卡片，聚焦接口地址、脱敏 Key 与五行可用模型，并统一卡片高度和底部操作布局。
- CLI 与终端扫描改为动态验证已保存路径，并覆盖 PATH、登录 Shell、版本管理器和常见安装目录。
- CC Switch 导入按目标区分 OpenAI 与 Anthropic 地址，并为 Codex 等 OpenAI 目标补全 `/v1`。
- 临时 CLI 只展示实际扫描到的终端，不再提供 Auto、Default 或自定义命令选项。

### 修复

- 修复凭据补全、刷新、签到和能力探测中的异步竞态，避免旧账号或旧请求覆盖当前配置。
- 修复协议切换后残留旧身份、额度、能力、签到和 API Key 元数据的问题。
- 修复 Sub2API Refresh Token、访问令牌过期时间及 Key 前缀处理不完整的问题。
- 修复能力探测将请求失败显示为“不支持”、批量签到误计失败及 CLI 实例数量过滤错误的问题。
- 修复 Windows 临时 CLI 对路径、参数、特殊字符和 `.cmd/.bat` 启动处理不完整，导致状态写回或清理失效的问题。
- 修复窗口缩放事件缺失时无法完成网格吸附，以及关闭工作空间选择器后旧请求继续写回的问题。
- 更新前端类型检查及其传递依赖，清除已知高危依赖告警。

## 0.3.6

### 新增

- 新增账号密码认证，可在登录后自动补全 Cookie、访问令牌和 API Key，并保留已有凭据供后续切换。
- 新增已有 API Key 同步与本地缓存，最多读取 100 个 Key，支持多 Key 选择主 Key，并保存额度、分组、限制和时间等元数据。
- 新增 Agent 与终端检测卡片，展示官方图标、名称和版本，并支持更多本地终端。

### 变更

- 重构中转站编辑窗口和应用设置窗口，改为分步工作区与即时保存布局。
- 认证方式选择始终完整展示，凭据补充区按账号密码、Cookie、访问令牌、API Key 的层级关系展开。
- 卡片操作按当前认证能力动态展示；API Key 仅保留 Key 额度、模型、测活和 CLI 相关能力，不再展示账号级管理与数据入口。
- API Key 额度始终按 Key 维度查询，即使本地保留其他账号凭据也不会越权回退。

### 修复

- 修复 API Key 模式填写 User ID 后被误认为具备账号管理能力的问题。
- 修复认证方式切换时主认证按钮逐级消失、需要额外点击切换入口才能恢复的问题。
- 修复 API Key 卡片可能显示过期账号 ID、账号额度或账号操作入口的问题。

## 0.3.5

### 新增

- 中转站卡片直接提供签到、刷新、编辑、启用/停用、删除等常用操作，并按能力展示复制、数据、站点、CLI 和 CC Switch 功能。
- 卡片和顶部工具栏统一使用语义化线性图标，新增中转站入口突出显示。

### 变更

- 移除中转站右键菜单，所有常用操作迁移到卡片内；保留多选项能力的轻量二级面板。
- 移除手动测活入口和相关命令，自动测活调度、记录和明细继续保留。
- NewAPI 修改密码允许首次设置密码时原密码为空，由服务端根据账号状态校验。

### 修复

- 修复应用更新弹窗无法读取 Release 正文，导致更新内容为空的问题。

## 0.3.4

### 修复

- 修复 macOS 专用临时 CLI 窗口定位代码在 Linux 和 Windows 上触发 Clippy `dead_code` 错误的问题。
- 修复 Codex 或 Claude Code 升级后已保存的可执行路径失效，必须手动重新扫描才能继续使用的问题。
- 修复 macOS 和 Linux 从桌面 App 启动临时 CLI 时未加载用户登录 Shell 环境，导致 Java、Maven、NVM 等命令不可用的问题。
- 修复已经退出的临时 CLI 仍作为历史记录保留并显示在卡片和实例抽屉中的问题。

### 变更

- 临时启动 CLI 改用 App 内工作空间选择器，支持目录浏览、路径输入、隐藏目录，以及按使用频率快速启动已记录的工作空间。
- 中转站卡片新增 CLI 默认配置切换，可按需更新 Codex 或 Claude Code 配置中的中转站 URL 与 API Key，并保留其他配置内容。
- 活动 CLI 存在时自动刷新实例生命周期，卡片展示当前数量，实例抽屉可打开支持定位的终端窗口。
- Release 在上传各平台安装包前执行 Rust 格式检查、Clippy 和测试，质量门禁未通过时不再发布版本。

## 0.3.3

### 变更

- 重新设计应用内更新提示，按结构展示版本变化与更新内容，并补充下载进度、安装状态和长内容滚动。

### 修复

- macOS 无标题栏模式不再强制重定位原生窗口按钮，并在系统菜单中补充全屏切换，避免进入全屏后缺少返回路径。

## 0.3.2

### 新增

- 中转站卡片补充 NewAPI 用户名，并重新组织账号、余额、认证方式和运行状态的信息层级。
- 只读解析 Codex 与 Claude Code 配置文件，根据中转站地址和 API Key 识别当前默认中转站。
- 新增临时 CLI 实例管理：记录 CLI 类型、终端、工作目录、PID、启动与退出时间、退出码，并支持重新启动。
- macOS 使用 Ghostty 启动临时 CLI 时记录稳定终端标识，可从实例抽屉精确打开对应窗口。

### 变更

- 中转站卡片改为可扩展的状态标签，不再平铺展示全部能力。
- 测活只保留 Codex 与 Claude Code CLI，移除 HTTP 测活调用和相关展示。

## 0.3.1

### 修复

- 修复 NVM 安装的 Codex CLI 被解析为包内脚本后无法找到 Node，导致本地扫描误判为不可用的问题。

## 0.3.0

### 新增

- 设置中新增「自启后最小化」开关：开机自启动时不弹出主窗口，仅保留托盘图标，点击托盘随时打开。

### 变更

- macOS：关闭主窗口后自动隐藏 Dock 图标，应用退为纯托盘形态；从托盘重新打开时恢复显示。

## 0.2.0

### 修复

- 自动签到失败不再全天反复重试并连发通知：改为每天最多 3 次、间隔 30 分钟，当天仅首次失败发一条通知。
- 后台自动刷新不再覆盖同一时间用户正在编辑保存的中转站配置；设置抽屉打开期间，后台同步不再清空未保存的修改。
- 授权自动测活后再保存设置，不再丢失授权记录。
- 测活 CLI 超时改为按进程组整树终止，不再遗留继续消耗额度的孤儿进程。
- 测活运行期间不再因 CLI 自动探测卡住其他操作。
- 修复 AnyRouter 子路径部署时接口地址被截断的问题。
- 手动刷新失败现在会明确提示原因；通知测试逐个渠道报告失败，不再被“部分成功”掩盖。
- 看板已有数据时，后台一次瞬时加载失败不再切换到全屏错误页。

### 新增

- 配置结构升级迁移：旧版本 data.json 会先自动备份再迁移，不再要求重新初始化。
- 站点刷新失败、测活由正常转为失败时发送通知（状态翻转只发一次，不重复打扰）。
- 设置中新增「重置自动测活授权」入口；开关已开但授权缺失时提供重新授权入口。
- HTTP 测活结果区分凭据失效与限流/站点故障，避免瞬时故障被误读为账号失效。
- 启动时自动清理残留的临时 CLI 目录，临时凭据文件权限收紧为仅当前用户可读。

### 变更

- 手动刷新与自动调度刷新互斥，避免对同一批站点重复请求；批量刷新与测活改为滚动并发。

## 0.1.0

### 新增

- NewAPI 兼容中转站账号管理。
- 余额刷新、签到、用量趋势、请求日志、API Key 管理和修改密码流程。
- Codex / Claude Code 测活，支持中转站单独覆盖全局配置。
- 配置导入导出，支持跨设备迁移。
- 系统通知和 Webhook 通知。
- 基于 GitHub Releases 和 Tauri Updater 的自动更新。
- macOS、Windows、Linux 的 x64 和 ARM64 发布包。
